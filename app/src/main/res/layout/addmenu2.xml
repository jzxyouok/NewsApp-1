<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/cover_relativelayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <LinearLayout
        android:id="@+id/cover_linearlayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:background="@drawable/linearlayout_stroke6"
        android:orientation="vertical">

        <RadioGroup
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_margin="10dp"
            android:orientation="horizontal"
            android:paddingBottom="10dp"
            android:paddingLeft="10dp"
            android:paddingRight="10dp"
            android:paddingTop="20dp">

            <RadioButton
                android:id="@+id/layout_write"
                style="@style/animation_menu"
                android:drawableTop="@mipmap/write"
                android:drawablePadding="10dp"
                android:text="文字" />

            <RadioButton
                android:id="@+id/layout_photo"
                style="@style/animation_menu"
                android:drawableTop="@mipmap/photo"
                android:drawablePadding="10dp"
                android:text="照片" />

            <RadioButton
                android:id="@+id/layout_photography"
                style="@style/animation_menu"
                android:drawableTop="@mipmap/photography"
                android:drawablePadding="10dp"
                android:text="视频" />

            <RadioButton
                android:id="@+id/layout_question"
                style="@style/animation_menu"
                android:drawableTop="@mipmap/question"
                android:drawablePadding="10dp"
                android:text="问题" />

        </RadioGroup>

        <LinearLayout
            android:id="@+id/layout_multiply"
            android:layout_width="match_parent"
            android:layout_height="50dp"
            android:clickable="true"
            android:layout_gravity="center_horizontal"
            android:gravity="center"
            android:background="@color/colorDarkWhite"
            android:orientation="vertical">

            <ImageView
                android:id="@+id/btn_multiply"
                android:layout_width="18dp"
                android:layout_height="18dp"
                android:contentDescription="@null"
                android:src="@mipmap/multiply" />
        </LinearLayout>
    </LinearLayout>
</RelativeLayout>